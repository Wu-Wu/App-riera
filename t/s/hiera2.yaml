---
:backends:
   - redis

:logger: console

:hierarchy:
   - %{environment}/%{fqdn}
   - modules/%{calling_module}
   - global

:yaml:
   :datadir: /usr/local/etc/puppet/hieradata

:puppet:
   :datasource: vars
